services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - DATA_DIR=/data
      - CHROMA_PERSIST_DIR=/data/chroma
      - SQLITE_PATH=/data/store.db
    volumes:
      - doc_system_data:/data

volumes:
  doc_system_data:
